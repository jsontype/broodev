# AdSense 심사 통과 기준 · 준수 현황

> **목적** — broodev.com 이 Google AdSense "가치가 별로 없는 콘텐츠"로 **두 번** 탈락한 뒤,
> 관련 Google 정책 전문을 항목별로 대조하고 **무엇을 어떻게 고쳤는지**를 증거와 함께 기록한다.
> 재심사 전 체크리스트이자, 앞으로 새 앱을 만들 때 지켜야 할 규칙이다.
>
> 최종 갱신: 2026-07-16 · 대상: `broodev.com`(루트=코인 시그널), `voca.broodev.com`
> **상태: 2026-07-16 모든 조치 완료·라이브 검증 후 재심사(검토) 요청 제출.** 경과는 §0-B.

---

## 0-D. 2026-08-07 구조 전환 완료 — 루트=VOCA, 코인=전용 서브도메인 (라이브 검증 21도메인 ✅)

- **루트 교체**: broodev.com = VOCA(콘텐츠 페이지 10+개 보유 학습 앱). 비트코인 앱은
  btc.broodev.com 전용으로 이동. 사이드 광고 레일 2개(1320px+) 예약.
- **코인 14종 복원**: apps/<sub> 스텁(301 _redirects)을 실제 코인 앱으로 교체
  (gen_coin.py v2 — 보호구역 방식). 각자 도메인 직접 서빙, canonical/title/OG
  자기 도메인화 + sitemap 핵심 3 URL 만 등재 → 과거 "91% 중복 15개 색인" 재발 방지.
- **검증**: scripts/verify-coins.mjs(14앱×30검사)·verify-runtime(19케이스) 통과,
  라이브 21도메인 전수 200 + 코인별 제목 확인 (2026-08-07).
- **다음**: 서치콘솔 broodev.com 색인 재요청 → 1~2주 후 AdSense 검토 요청.
  (선택) voca.broodev.com → broodev.com 301 존 규칙.

## 0-C. 2026-08-07 심사 결과 — 반려: "가치가 별로 없는 콘텐츠" (low value content)

- 소유권 확인 ✅ (통과 — 지난 조치들이 무효는 아니었음). 반려 사유는 **정책 위반 계열이 아니라 콘텐츠 평가**.
- 진단: 루트(broodev.com)가 유틸리티 대시보드라 크롤러 관점의 **독창적 텍스트 콘텐츠가 얇음**.
  "low value content"는 툴 단독 사이트의 최빈 반려 사유로, 클론/캐시/리디렉션 수정과는 별개 축.
- 대응 방침(확정 시 §0-D로 기록):
  1) 루트에 **정적 HTML 콘텐츠 허브**(/guide/) 신설 — 지표 해설·투자 기초 등 원문 아티클 15~20편,
     상호 링크 + 메인 내비 노출 + sitemap 등재. JS 렌더 의존 금지(정적 텍스트).
  2) 색인이 잡히도록 **2주 안팎 운영 후** 재검토 요청 (변경 없이 재요청 반복 금지 — 사이클만 소모).
  3) 재검토 제출일·결과는 이 문서에 계속 기록.

## 0-B. 2026-07-16 마무리 — 리버트 복구 · 라이브 검증 · 검토 요청 제출

### 경과
1. 대응 PR(#13) 머지 직후 **사이트 전체 백지** 발생 → 사용자가 즉시 리버트(#14)
2. 원인: `apps/btc/index.html` 에서 `const COIN` 을 **선언보다 위에서 참조**한 TDZ(`ReferenceError`) — React 렌더가 통째로 죽음
3. 수정본 재적용 + 이 문서의 잔여 항목 처리 → PR #15 머지 (2026-07-16)
4. Cloudflare 잔여 작업(§9-C) 완료 → **라이브 전수 검증** → AdSense 검토 요청 제출

> ⚠️ **TDZ 교훈 — "babel 컴파일 OK"는 아무것도 보장하지 않는다.**
> TDZ 는 컴파일이 아니라 **런타임** 에러다. `index.html` 을 고치면 반드시
> `node scripts/verify-runtime.js` (앱을 스텁 환경에서 **실제 실행**) 를 돌려라. 부록 A 참조.

### PR #15 에서 추가로 고친 것 (2026-07-15~16)

| 항목 | 내용 |
| --- | --- |
| 게이트 자백 주석 | view-source 로 읽히는 "6~9초 광고 노출 연출" 상세 서술을 한 줄 요약으로 축약. 고아 `.gate-btn` CSS 삭제 |
| methodology 밴드 표 | 코드(`labelMom`)와 불일치 정정: 단기 65~80=**BUY**, 25~45=**WEAK**, 0~25=**AVOID**. "최하단 밴드만 다르다"는 거짓 서술 수정 |
| 블로우오프 톱 가드 | 코드에만 있던 둘째 항 **F&G≥90 → `(F&G−90)×2.0`점 차감** 을 문서화. UI에 없는 "가드 표시" 서술은 쓰지 않음(코드 확인 결과 렌더 안 됨) |
| 콘텐츠 라벨 | `drawdown-dca.html` 0~25 를 탭별 병기(장기 OVERHEATED/단기 AVOID), `guide-fear-greed.html` 표에 "장기 탭 기준" 명시 |
| voca 404 | btc 복사본(암호화폐 링크)이던 것을 voca 페이지로 교체 |
| voca about | 365단어 → **본문 2,450자+**. localStorage·백업 JSON·TTS·문의 스크린샷 KV 90일 예외·미출시 프리미엄 선제 고지 — 전부 코드에서 확인한 사실만 |
| clean URL 잔여 | 프래그먼트·쿼리 붙은 내부 `.html` 링크 17건 치환(308 홉 제거), 앵커 실존 검증 |
| 헤더 내비 | 13개 언어 i18n + 언어 선택기 헤더 이동 + 모바일 햄버거 |
| `*.pages.dev` | `functions/_middleware.js` 가 pages.dev 호스트에 `X-Robots-Tag: noindex, nofollow` 부착 (§9-C-②를 **코드로** 해결) |
| 업로드 API | `shot.js` Turnstile 검증(옵트인 — `TURNSTILE_SECRET` 설정 시 활성화, 미설정 시 기존 동작) |

### Cloudflare 잔여 작업 처리 결과 (§9-C 대응)

| §9-C | 결과 |
| --- | --- |
| ① `btc.broodev.com` 클론 | ✅ **`broodev-btc` 프로젝트 + DNS 삭제** — 호스트 자체가 소멸(curl 미해석 확인) |
| ② `*.pages.dev` | ✅ 미들웨어 noindex 로 해결 — 라이브에서 헤더 확인, 커스텀 도메인엔 헤더 없음 확인 |
| ③ 코인 서브도메인 301 | ✅ 라이브 확인 — `eth/xrp/doge/shib` 전부 `301 → broodev.com/?coin=<coin>` |
| ④ Root directory | ✅ `broodev-web` = `apps/btc` 확인 |
| ⑤ 업로드 API | ✅ 코드 구현(옵트인). **AdSense 심사와 무관한 보안 항목**이라 활성화는 보류 — 절차는 §9-C-⑤ |

### 🔴 /adsense/ 유령 캐시 사건 — 반드시 기억할 것

옛 배포가 `Cache-Control: public, s-maxage=604800`(7일) 로 캐시해 둔 `broodev.com/adsense/` 응답(광고 코드 포함)이
**Purge Everything → Custom Purge(URL) → Pages 재배포를 전부 무시하고** 계속 서빙됐다
(`Cf-Cache-Status: DYNAMIC` 인데 `Age` 는 증가하는, 퍼지가 안 닿는 캐시 층).

**해결: 존 Redirect Rule** — `starts_with(http.request.uri.path, "/adsense")` → `https://broodev.com/` 301.
**Redirect Rule 은 캐시 조회보다 먼저 실행**되므로 어떤 캐시 층에 뭐가 남아 있든 이긴다. 즉시 반영 확인.

> 교훈 ①: 광고 달린 페이지를 지웠으면 **라이브에서 슬래시 유무 양쪽 다** 확인하라
> (`/adsense` 404 ≠ `/adsense/` 404 — 캐시 키가 다르다).
> 교훈 ②: 퍼지로 안 죽는 캐시는 싸우지 말고 **Redirect Rule 로 앞단에서 차단**하라.
> 교훈 ③: 새 배포는 `max-age=0, must-revalidate` 라 재발 없음 — 캐시 규칙을 새로 만들 때 HTML 장기 캐시 금지.

### 검토 요청 제출 판단 기록
"색인에서 옛 클론이 빠질 때까지 1~2주 대기"도 검토했으나, **클론 URL 접근 시 이미 301 이 보이는 것 자체가 수정 증거**이고
라이브 상태에 잔여 위반이 없음을 전수 확인했으므로 **즉시 제출**했다. (§8의 대기 권고는 "라이브가 아직 옛 상태일 때" 기준)

---

## 0-A. ⚠️ 2026-07-15 전수 감사 — 이전 판의 주장 중 상당수가 틀렸다

7개 렌즈 병렬 감사에서 **79건**이 나왔다(CRITICAL 16 · HIGH 27). 이 문서의 이전 판이
**"광고 게재 화면은 btc·voca 2개뿐"** 이라고 단언했는데 **거짓이었다.**
로더 스크립트(`googlesyndication`)만 grep 했고, **렌더링되는 `<ins class="adsbygoogle">` 태그와
`ads.txt` 를 확인하지 않았다.**

### 이번에 잡아낸 것 중 가장 심각했던 것들

| 문제 | 실체 |
| --- | --- |
| **가짜 계산 게이트** | 데이터를 이미 다 받아놓고 **6~9초 가짜 "계산 중" 연출**(`total = 6000 + rand(3000)`)을 돌렸다. UI 문구가 13개 언어 전부에서 **"그동안 아래 광고가 노출됩니다"** 라고 명시. 코드 주석이 스스로 **"연출"** 이라 불렀다. STALE 되면 점수를 다시 잠가 **게이트 반복 통과**(= 광고 반복 노출). → **광고 노출을 인위적으로 만드는 장치.** 전면 삭제 |
| **광고 화면 ≠ 콘텐츠 화면** | 원본 콘텐츠 12편을 썼는데 **그 페이지엔 광고가 없었다.** AdSense 가 심사하는 "광고가 게재되는 화면"은 여전히 대시보드 하나뿐이었다. → **콘텐츠 페이지 18장에 AdSense 추가.** 이제 광고 화면 = 콘텐츠 화면 |
| **빈 광고 슬롯** | `data-ad-slot=""` 로 600px "AD" 라벨 빈 박스 2개를 렌더 + `***! TODO` 주석과 `여기에_광고단위_슬롯ID` 플레이스홀더가 view-source 에 그대로 노출 → **미완성 화면.** RailAd·플레이스홀더 전부 삭제 |
| **클론 "광고 제거"가 미완** | 로더만 지웠고 `<ins data-ad-client="ca-pub-…">` 는 여전히 렌더됐다. **`ads.txt` 19개**가 pub ID 를 선언 중이었다 → 클론 콘텐츠 **전부 삭제** + 301, `ads.txt` 는 btc·voca 만 |
| **`renderSEO()` 가 내부링크를 지움** | 언어 전환 시 `section.seo` 를 통째로 덮어써서 **새로 추가한 콘텐츠 링크가 사라졌다** → `build()` 에 링크 블록 추가 |
| **`dev` 가 코인 클론을 다시 링크** | 통합 sed 가 `app.jsx` 를 놓쳐 dev 포털이 14개 클론을 전부 링크 중이었다 → 루트 `?coin=` 로 교체 |
| **soft-404** | 존재하지 않는 URL 전부가 **HTTP 200 + 광고 붙은 홈페이지**를 반환 → `404.html` + `_redirects` 로 진짜 404 |
| **크롤 차단 함정** | `admin` 의 `robots.txt Disallow: /` 가 크롤을 막아 **noindex 가 영영 적용되지 않음**(색인에서 안 빠짐) → `Allow: /` 로 수정. 코인 클론도 같은 이유로 `Allow`(301 을 크롤러가 봐야 통합됨) |
| **내부 문서 공개 서빙** | `broodev.com/README.md` 로 내부 TODO·**프리미엄 우회 치트**가 공개되고 있었다 → 배포 디렉터리에서 삭제 |

> **교훈: `index.html` 만, 로더 스크립트만 grep 하지 마라.**
> **모든 `*.html`** 에서 **렌더링되는 광고 마크업(`<ins>`, `data-ad-client`)과 `ads.txt`** 까지 봐야 한다.

---

## 0. 탈락 이력과 진짜 원인

| 시점 | 사유 | 우리가 처음에 내린 진단 | **실제 원인** |
| --- | --- | --- | --- |
| 1차 (2026-07-01) | 가치가 별로 없는 콘텐츠 | "루트가 얇은 포털이라서" | 부분적으로만 맞음 |
| 2차 | 가치가 별로 없는 콘텐츠 | 루트를 btc 앱으로 교체했는데도 탈락 | **property 전체가 문제였음** |

**진짜 원인:** 코인 앱 **15종이 전부 2277줄, 서로 91% 동일**(코인 이름만 치환)한 채
**전부 `index,follow` + AdSense 게재** 상태로 서브도메인에 배포되어 있었다.
이는 Google 스팸 정책이 문자 그대로 금지하는 **"쿠키 커터(cookie-cutter) 도어웨이 페이지"** 다.

> ⚠️ **핵심 교훈 — 심사 단위는 "페이지"가 아니라 "광고가 게재되는 화면 전체(property)"다.**
> 루트만 아무리 잘 만들어도 서브도메인에 얇은/복제 광고 페이지가 있으면 끌려 내려간다.
> "루트에 어떤 앱을 두느냐"는 결정 변수가 **아니었다.**

---

## 1. 고유하고 충분한 콘텐츠

> **Google:** "페이지에 고유 콘텐츠가 충분해야 합니다. 사용자가 내 사이트를 방문할 수밖에 없고 다시 방문하고 싶게 만드는 콘텐츠를 제공해야 합니다."
> "콘텐츠에서 실질적인 가치를 제공하는지, 유사한 주제를 다루는 다른 사이트와 비교하여 독창적인지 스스로 자문해 보십시오."

### 조치
광고를 게재하는 두 사이트에 **원본 해설 콘텐츠 7편(약 65,000자)** 을 신규 작성했다.
전부 직접 쓴 글이며, **템플릿 복제가 아니다.**

**`broodev.com` (코인 시그널) — 색인 7페이지 / 47,793자**

| 페이지 | 분량 | 왜 고유한가 |
| --- | --- | --- |
| `methodology.html` | 11,446자 | **우리 모델의 내부를 공개한 설계 문서.** 6개 지표 정규화 함수, 단기(모멘텀)/장기(역발상) **두 가중치 세트의 실제 수치**, 결측 지표 재정규화, 5단계 밴드, 그리고 **이 모델의 한계 6가지**. 다른 사이트가 쓸 수 없는 글이다. |
| `indicators.html` | 8,858자 | 지표 6개를 "무엇을 재는가 / 계산식 / 읽는 법 / **언제 배신하는가**" 로 해설. 마지막 항목이 차별점. |
| `guide-fear-greed.html` | 7,291자 | 공포 구간 분할매수 절차, 3중 확인(심리·밸류·추세), 초보의 5가지 실수 |
| `glossary.html` | 6,312자 | 지표·전략 용어집 |
| `index.html` | 8,813자 | 대시보드 + 정적 SEO 본문 + FAQ |
| `privacy.html` / `terms.html` | — | 정책 |

실제 코드의 가중치를 그대로 문서화했다 (README의 옛 수치가 아니라):
- **단기(모멘텀)**: RSI 28 · F&G 28 · DD 18 · MACD 14 · GC/DC 8 · Mayer 4
- **장기(역발상)**: RSI 18 · MACD 18 · F&G 18 · Mayer 18 · DD 14 · GC/DC 14

**`voca.broodev.com` (단어암기장) — 색인 6페이지 / 31,672자**

| 페이지 | 분량 | 내용 |
| --- | --- | --- |
| `method.html` | — | 깜빡이 암기법의 원리 — 에빙하우스 망각곡선, 간격 효과, 이중 부호화, 인지 부하. **그리고 "수동 노출은 친숙함이지 인출이 아니다"는 한계와 보완법** |
| `csv-guide.html` | — | CSV 단어장 실전 가이드 — 한글 깨짐(CP949/UTF-8) 해결, 쉼표 이스케이프, 좋은 카드 설계 5원칙 |
| `study-guide.html` | — | 학습 전략 — 하루 몇 개, 복습 주기, 인터리빙, 4주 계획, 흔한 실수 |

### ⚠ 남은 약점 (정직하게)
- 65,000자는 **개선됐지만 압도적이지 않다.** 승인된 다수 사이트는 이보다 콘텐츠가 많다.
- **"정기적 업데이트"** 요건(Google: *"새로운 고유 콘텐츠를 꾸준히 추가"*)을 아직 못 채운다.
  → **다음 단계: 정기 시장 논평/학습 팁 등 신선한 콘텐츠 축적.**

---

## 2. 중복 콘텐츠 없음

> **Google:** "독창적이거나 가치 있는 콘텐츠를 추가하지 않고 다른 사이트의 콘텐츠를 복사하여 다시 게시하는 사이트"
> "**동의어로 대체하거나 자동화된 기법을 사용하는 등의 방법으로 다른 사이트의 콘텐츠를 복사하여 약간 수정한 후 다시 게시하는 사이트**"
> "여러 페이지가 유사하거나 매우 유사한 콘텐츠가 많은 경우 각 페이지를 확장하거나 **하나로 통합**하는 것이 좋습니다."
> "동일한 페이지 내에서 또는 **사이트의 여러 페이지 전반에서 동일한 콘텐츠가 중복되지 않도록** 합니다."

### 이것이 우리의 실제 탈락 사유였다
코인 15종을 **코인 이름만 치환**해 양산한 것이 정확히 위 문구에 해당한다.

### 조치 — 15개 → 1개로 통합
Google 이 권고한 **"하나로 통합"** 을 그대로 실행했다.

| 항목 | 이전 | 현재 |
| --- | --- | --- |
| 색인되는 코인 페이지 | **15개** (91% 동일) | **1개** (루트) |
| 광고 게재 코인 페이지 | **15개** | **1개** (루트) |
| 코인 전환 방식 | 서브도메인 복제본 15개 | **한 앱 안의 파라미터** `broodev.com/?coin=eth` |
| 클론 간 상호링크 | 각 클론이 14개 형제를 링크 | **전부 제거** |

- 루트 `apps/btc` 에 **코인 레지스트리(15종) + 선택기** 추가 → 앱 하나가 전 코인 서빙
- 코인 서브도메인 14개: **AdSense 제거** + `noindex` + `canonical→루트` + `sitemap.xml` 삭제 + 상호링크 제거
- 코인 클론의 `privacy/terms` 28장도 `noindex` (본체는 noindex 인데 하위 정책 페이지만 색인되던 문제)

### 추가로 잡아낸 중복 (전수 감사에서 발견)
- **`apps/btc/adsense/`** — 루트의 **구 광고버전 고아 폴더**. `broodev.com/adsense/` 로 **실제 라이브 서빙 중**이었고
  **광고 게재 + `index,follow` + canonical 이 낡은 `btc.broodev.com`** 이었다.
  즉 구글에게 **루트와 동일 콘텐츠의 광고 페이지가 2장** 보이고 있었다.
  → 광고 제거 + `noindex,nofollow` + canonical→루트
- **`apps/voca-backup/`** — voca 와 89% 동일한 롤백 스냅숏이 광고+색인 상태 (현재 DNS 미배포)
  → 광고 제거 + `noindex` (배포되어도 안전하도록)
- **`apps/btc/member/`** — canonical 이 낡은 서브도메인 → 루트로 교체

> ⚠️ **`noindex` 만으로는 AdSense 가 해결되지 않는다.**
> `noindex` 는 **검색 색인** 지시일 뿐이고, AdSense 정책은 **광고가 게재되는 화면**에 적용된다.
> **광고 재고에서 빼는 것(스크립트 제거)** 이 실질적 조치다. 둘 다 했다.

### ✅ 완료 (2026-07-16 라이브 확인)
`<coin>.broodev.com` → `broodev.com/?coin=<coin>` **301 라이브 작동 확인** (`apps/<coin>/_redirects`).
`btc.broodev.com` 은 프로젝트·DNS 삭제로 **호스트 자체 소멸**. 상세는 §0-B.

---

## 3. 탐색 요소 · 사용자 환경

> **Google:** "만족스러운 사용자 환경을 제공하려면 **쉽게 찾고 사용할 수 있는 탐색 메뉴(메뉴 바)** 를 갖추는 것이 중요합니다."
> "**메뉴 바에 탐색 요소를 추가하고 주제별, 카테고리별로 콘텐츠를 구성**하면 사용자가 내 사이트를 파악하고 탐색하는 데 도움이 됩니다."
> 점검 항목: 정렬(모든 기기) · 가독성 · 기능(클릭 가능) · **정확성(알맞은 콘텐츠로 연결)**

### 조치 — 탐색 메뉴 신설
**이 요건은 전수 감사에서야 발견됐다.** 그전까지 콘텐츠 링크가 **푸터에만** 있었고 메뉴 바가 없었다.

- `broodev.com` 상단에 `<nav class="site-nav">` 추가: **대시보드 · 점수 방법론 · 지표 해설 · 공포지수 활용법 · 용어집**
- `voca.broodev.com` 상단에 추가: **암기장 · 암기법 원리 · CSV 만들기 · 학습 전략**
- 콘텐츠 페이지 7편에도 각각 상단 내비 존재
- `#root` **바깥**에 두어 React 렌더·언어 전환에도 사라지지 않고, **JS 없는 크롤러도 읽는다**
- 모바일 대응(`@media max-width:560px`), `aria-label`/`aria-current` 접근성 속성 포함

**정확성 검증:** 메뉴의 모든 링크가 실존 파일을 가리킴 (자동 검사 통과, 깨진 링크 0)

### 그 외 요건
- ✅ **누락된 페이지로 연결되는 링크 없음** — 내부 링크 전수 검사 통과
- ✅ **여러 브라우저 표시** — 표준 HTML/CSS, CDN React. 반응형
- ✅ **사이트 테마와 무관한 페이지 없음** — 코인 시그널 / 단어암기로 주제 일관

---

## 4. 클로킹 · 부적절한 리디렉션

> **Google:** "클로킹이란 Googlebot 에 제공되는 콘텐츠와 사용자에게 제공되는 콘텐츠가 다른 경우를 말합니다."
> "조건적으로 리디렉션하는 URL이 있는지 확인합니다. 이 유형의 리디렉션은 주로 **자바스크립트**로 작성됩니다."
> "**구독 및 페이월 콘텐츠**: 게시자는 Google에서 페이월 콘텐츠와 클로킹 행위를 구분할 수 있도록 **페이월 콘텐츠를 구조화된 데이터에 포함**해야 합니다."

### 검증 결과

| 항목 | 상태 | 증거 |
| --- | --- | --- |
| 정적 SEO 본문이 사용자에게도 보이는가 | ✅ **보임** | `.seo { position:relative; z-index:2; ... }` — `display:none` 아님. 크롤러와 사용자가 같은 것을 본다 |
| CSS 로 숨긴 키워드 텍스트 | ✅ **없음** | `visibility:hidden` 은 **호버 툴팁(`.tip`)** 에만 적용 — 정상 UI 패턴 |
| 배경색과 같은 색 텍스트 | ✅ 없음 | |
| 조건부 JS 리디렉션 | ⚠️ **존재** | `apps/btc/index.html`: `localStorage.getItem('btc:member')==='1' → location.replace('member/')` |
| 페이월 구조화 데이터 | ⚠️ **주의** | `apps/voca`: `"isAccessibleForFree": true` — 현재 `PREMIUM.launch = false` 라 **사실과 일치**함 |

### ⚠️ 조건부 리디렉션에 대한 판단
btc 의 `member/` 리디렉션은 **이용자가 스스로 `?member=1` 로 설정해야만** 발동한다.
Googlebot 과 **일반 신규 방문자는 동일한 페이지**를 본다 → 전형적 클로킹은 아니다(페이월 동작에 가깝다).
`member/` 는 `noindex, nofollow` 이고 광고도 없다.

**그러나 리스크는 남는다:**
- 이 기능은 **미완성**이다 (코드 주석: `***! TODO: 실제 구독 검증은 결제+인증 백엔드로. 지금은 임시 클라이언트 플래그(우회 가능, 보안 아님)`).
- AdSense 는 **"아직 미완성 상태인 화면"** 도 금지한다.

**✅ 권고 1안 실행 완료** — `member/` 자동 리디렉션(`btc:member` 플래그) 제거됨.
`member/` 는 `noindex` + 광고 없음 상태로 남아 있고, 결제/구독을 실제로 출시할 때
페이월 구조화 데이터(`isAccessibleForFree: false` + `hasPart`/`cssSelector`)와 함께 재도입한다.
`voca` 의 `isAccessibleForFree: true` 는 `PREMIUM.launch = false` 인 현재 사실과 일치 — 프리미엄 출시 시 **반드시 함께 갱신**.

### 이미지 클로킹 · 모바일 리디렉션
- ✅ **이미지 클로킹 없음** — 사용자와 검색결과에 동일 이미지. `functions/_middleware.js` 는 `?lang=` 에 따라 OG 메타를 바꾸지만, **같은 URL 이면 누구에게나 같은 응답**이라 클로킹이 아니다.
- ✅ **모바일 전용 리디렉션 없음** — user-agent 기반 분기 없음.
- ⚠️ 코인 클론들의 OG 썸네일이 btc 것 복사 상태 (README TODO). 301 로 클론을 없애면 자연 해소.

---

## 5. Google 웹 검색 스팸 정책

> **Google:** "사이트에 도움이 되지 않는 **불필요한 키워드나 반복 키워드**를 사용하면 안 됩니다."
> "검색엔진 전용으로 제작된 **'도어웨이' 페이지** 또는 자체 콘텐츠가 거의 또는 전혀 없는 **'쿠키 커터'** 접근 방식을 만들면 안 됩니다."
> "사이트에 자신의 소유가 아닌 콘텐츠 또는 서비스를 제공하며 소유권이 있는 것으로 주장해서는 안 됩니다."

| 항목 | 상태 | 조치 |
| --- | --- | --- |
| **키워드 반복 / 스터핑** | ✅ 해소 | `<meta name="keywords">` 가 **18개 앱**에 있었고, `비트코인 공포지수, 비트코인 공포탐욕지수, 공포탐욕지수 실시간, 비트코인 매수 타이밍, 비트코인 지금 사도 되나...` 처럼 **같은 키워드의 변형을 나열**하는 패턴이었다. → **전부 삭제** (Google 은 meta keywords 를 랭킹에 쓰지 않으므로 SEO 손실 0, 스팸 신호만 제거) |
| **도어웨이 / 쿠키 커터** | ⚠️ **부분 해소** | 코인 클론 14개가 정확히 "쿠키 커터"였다. 광고 제거·noindex·통합은 완료. **완전 해소하려면 301 필요** (§2 참조) |
| **제3자 콘텐츠 소유권 주장** | ✅ 문제 없음 | 시세·지표 데이터의 **출처(CoinGecko·Binance·Alternative.me)를 페이지에 명시**하고, 우리는 그 위에 **자체 합성 점수와 원본 해설**이라는 부가가치를 얹는다. 데이터 소유권을 주장하지 않는다 |
| **숨겨진 텍스트** | ✅ 없음 | §4 참조 |

---

## 6. AdSense 프로그램 기본 요건

| 항목 | 상태 | 증거 |
| --- | --- | --- |
| **개인정보처리방침** (쿠키·AdSense 고지 포함) | ✅ | `/privacy.html` — 모든 광고 게재 페이지의 **탐색 메뉴/푸터에서 도달 가능**. Google 광고 쿠키·맞춤광고 해제 링크 포함 |
| **이용약관** | ✅ | `/terms.html` |
| **연락처 / 운영주체** | ✅ | Y-Systems · jsontyper@gmail.com — 푸터 명시 |
| **`ads.txt`** | ✅ | 루트에 `google.com, pub-5511225478572825, DIRECT, f08c47fec0942fa0` |
| **`sitemap.xml`** | ✅ | 루트 7 URL (noindex 페이지 미등재) |
| **YMYL(금융) 면책** | ✅ | 전 코인 페이지·전 콘텐츠 글에 "투자 자문 아님 / 원금 전액 손실 가능 / DYOR" 고지 |
| **지원 언어** | ✅ | 한국어(주) — 지원 언어 |
| **미완성/공사중 화면** | ✅ | `member/` 자동 리디렉션 제거됨(§4). noindex + 광고 없음 |

---

## 7. 최종 상태 — 광고가 게재되는 화면

**AdSense 정책이 적용되는 화면은 이 둘뿐이다.**

| 사이트 | 색인 페이지 | 콘텐츠 | 탐색 메뉴 | 개인정보처리방침 |
| --- | --- | --- | --- | --- |
| `broodev.com` (루트, 코인 시그널) | 7 | **47,793자** | ✅ | ✅ |
| `voca.broodev.com` (단어암기장) | 6 | **31,672자** | ✅ | ✅ |

**광고가 없는 곳** (정책 적용 대상 아님):
코인 서브도메인 14개(전부 301→루트) · `dev`(noindex) · `voca-tutorial` · `voca-backup`(noindex·미배포) · `admin`(noindex) · `btc/member/`(noindex)

**소멸한 곳** (2026-07-16): `btc.broodev.com`(프로젝트·DNS 삭제) · `broodev.com/adsense/`(디렉터리 삭제 + 존 Redirect Rule 301) · `*.pages.dev`(noindex 헤더)

---

## 8. 재심사 전 체크리스트

### 🔴 배포 전 필수 — ✅ 전부 완료 (2026-07-16)
- [x] PR #15 머지 → Cloudflare 재배포
- [x] **코인 서브도메인 14개 → `broodev.com/?coin=<coin>` 301** — 라이브 확인 (eth/xrp/doge/shib 표본)
- [x] `member/` 페이월 리디렉션 제거 (§4 권고 1안)

### 🟡 배포 직후 검증 (라이브에서) — ✅ 전부 통과 (2026-07-16)
- [x] `broodev.com/` — 탐색 메뉴 표시, 대시보드 정상 렌더 (백지 아님)
- [x] `broodev.com/adsense/` — **존 Redirect Rule 로 301** (슬래시 유무 양쪽 다 — §0-B 유령 캐시 사건 참조)
- [x] `broodev.com/methodology` · `/indicators` · `/guide-fear-greed` · `/glossary` — 200 (clean URL)
- [x] `eth.broodev.com` — 301 로 루트 이동
- [x] `broodev.com/ads.txt` · `/sitemap.xml` — 200
- [x] `*.pages.dev` — `X-Robots-Tag: noindex, nofollow` / 커스텀 도메인엔 없음
- [x] 존재하지 않는 경로 — 진짜 404 (soft-404 아님)
- [x] 모바일에서 탐색 메뉴(햄버거) 정상 동작

### 🟢 재심사 요청 전 (가장 자주 실수하는 곳)
- [x] **라이브가 수정된 상태인지 먼저 확인.** 라이브가 옛 상태(예: 리버트된 master)면 Google 은 옛날 것을 본다.
      2026-07-16 은 라이브 전수 검증 후 즉시 제출 — 클론 URL 이 301 을 반환하는 것 자체가 수정 증거이므로
      색인 정리 대기는 생략했다 (§0-B 판단 기록).
- [ ] **Search Console 색인 페이지 수 확인** ← **가장 중요한 지표** (심사 대기 중에도 계속 모니터링)
      - **한 자릿수면 그 자체가 탈락 사유다.** 목표: 두 자릿수 이상
      - 클론 페이지(`eth.broodev.com` 등)가 색인에서 **빠지고 있는가**? — `site:eth.broodev.com` 으로 추적
      - 새 콘텐츠(방법론·지표 심층 6편 등)가 **색인됐는가**?
- [ ] **URL 검사 도구**로 Googlebot 이 보는 것 = 사용자가 보는 것 확인 (클로킹 자가진단)
- [ ] **수동 조치 보고서** 확인 (스팸 정책 위반 시 여기 표시됨)
- [x] **재심사 요청 제출** (2026-07-16) — 결과 대기 중. 탈락 시 §9 "다음 수" 실행

> Google 은 구체적 탈락 사유를 주지 않는다. **Search Console 이 유일하게 얻을 수 있는 실제 데이터다.**
> 여기를 안 보고 재신청하는 것은 눈 감고 다시 던지는 것과 같다.

---

## 9. 또 탈락하면 — 다음 수

Google 은 구체적 사유를 주지 않는다. 그러니 **변수를 줄여 신호를 명확히** 해야 한다.
(2·3번은 2026-07-16 에 이미 실행 완료 — 다음 탈락 시 남은 카드는 1·4·5·6 이다.)

1. **광고 게재 사이트를 1개로 줄인다.** 현재 2개(`broodev.com`, `voca`)라 어느 쪽이 문제인지 알 수 없다.
   콘텐츠가 더 많은 `broodev.com` 만 남기고 voca 광고를 빼면 신호가 명확해진다.
2. ~~클론을 301 로 완전 소멸시킨다~~ ✅ 완료 (§0-B)
3. ~~`member/` 미완성 페이월을 제거한다~~ ✅ 완료 (§4)
4. **콘텐츠를 더 쌓는다.** 65,000자는 통과선 근처지 안전선이 아니다.
   Google 이 요구하는 **"정기적 업데이트"** — 주간 시장 논평 등 — 를 실제로 축적한다.
5. Search Console 의 **수동 조치 보고서**를 확인한다 (스팸 정책 위반 시 여기에 표시된다).
6. **색인 정리를 기다린다.** 이번엔 즉시 제출했으므로, 탈락하면 `site:eth.broodev.com` 등
   옛 클론이 색인에서 완전히 빠진 것을 확인한 뒤(통상 2~4주) 재신청한다.

---

## 9-B. 실전 운영 관점 (현장 경험칙 대조)

### ① 색인 페이지 수 — **한 자릿수면 그 자체가 탈락 사유**
Search Console 의 색인 페이지 수가 한 자릿수면 Google 이 "이 사이트를 파악할 만큼의 콘텐츠가 없다"고 본다.

| 시점 | `broodev.com` 색인 가능 페이지 |
| --- | --- |
| 탈락 당시 | **3** (index + privacy + terms) ← 한 자릿수. **이것이 핵심 원인 중 하나였다** |
| 콘텐츠 4편 추가 후 | **7** ← 여전히 한 자릿수 |
| 심층 해설 6편 추가 후 | **13** ← 두 자릿수 진입 |

> ⚠️ **역설:** 코인 15→1 통합은 중복 위반을 없앴지만 **색인 페이지 수를 줄였다.**
> 그래서 **서로 진짜 다른** 심층 콘텐츠로 페이지 수를 다시 늘려야 했다.
> **템플릿 양산으로 페이지 수를 늘리는 것은 우리를 죽인 그 실수의 반복이다.** 절대 금지.

**재심사 전 반드시:** Search Console → 색인 생성 → **색인된 페이지 수 확인**.
Google 이 구체적 탈락 사유를 주지 않으므로 **이것이 유일하게 얻을 수 있는 실제 데이터다.**

### ② 툴 페이지마다 800자 이상 고유 설명 — ✅ 충족
도구만 덩그러니 있는 페이지는 "게시자 콘텐츠 없음"이다. 각 툴 페이지에 사용법·원리·FAQ 를 넣어야 한다.

| 툴 페이지 | 정적 본문 |
| --- | --- |
| `broodev.com/` (코인 대시보드) | **9,233자** ✅ |
| `voca.broodev.com/` (단어암기장) | **5,967자** ✅ |

### ③ 재심사 간격 — **2주 ~ 1개월**
> **아무것도 고치지 않고 재신청하면 자동으로 같은 사유로 또 떨어진다.**
> 더 중요한 것: **고쳤더라도 Google 이 재크롤하기 전에 신청하면 Google 은 옛날 상태를 본다.**

**재심사 신청 타이밍:**
1. 배포 완료
2. **2주~1개월 대기** — Google 이 재크롤·재색인할 시간
3. Search Console 에서 **확인**: 클론 페이지가 색인에서 빠졌는가? 새 콘텐츠가 색인됐는가?
4. **그 다음에** 재심사 요청

### ④ 프로그래매틱 페이지 — noindex 후 승인되면 해제? **우리는 예외다**

일반적으로는 정석이다: 프로그래매틱 페이지에 승인 전 `noindex` 를 걸고, 승인 후 해제한다.
**그러나 그것은 각 페이지가 진짜로 고유한 가치를 가질 때의 이야기다.**

**우리 코인 클론 14개는 91% 동일하다.** 승인 후 `noindex` 를 풀면:
- 91% 동일한 페이지 14개가 다시 색인 → **탈락했던 상태로 정확히 복귀**
- AdSense 는 **승인 후에도 지속적으로 단속**한다 → 나중에 정책 조치(수익 중단) 위험
- Google 웹검색 스팸 정책의 **"쿠키 커터"** 에 그대로 해당

**따라서 선택지는 둘뿐이다:**

| 선택 | 내용 | 결과 |
| --- | --- | --- |
| **A (권장)** | 복원하지 않는다. **301 로 루트에 영구 통합** | 안전. 코인 롱테일 SEO 는 포기 |
| **B** | **코인마다 고유 콘텐츠를 붙인 뒤** 복원 | 각 코인별 고유 해설/논평/데이터 분석이 있어야 "합법적 프로그래매틱"이 된다. 그때는 `noindex` 해제가 정당 |

> **`noindex` 해제는 콘텐츠를 채운 다음에나 꺼낼 수 있는 카드다.**
> 지금 상태로 해제하면 재발한다.

---

## 9-C. 레포에서 못 고치는 것 — Cloudflare 처리 목록 (✅ 2026-07-16 전부 처리 완료)

아래는 **코드로 해결이 안 되고 Cloudflare 콘솔에서 처리해야 하는 잔여 위험**이었다.
처리 결과 요약은 §0-B, 각 항목 상세는 아래.

### ① `btc.broodev.com` — 통합에서 누락된 15번째 클론 → ✅ 소멸
`apps/btc` 한 디렉터리가 **`broodev.com` 과 `btc.broodev.com` 두 호스트에 동시 배포**된다
(`broodev-web` + `broodev-btc` 두 Pages 프로젝트). 즉 심사 대상 루트의 **완전 복제본**이
광고를 달고 살아 있다. `_redirects` 로는 못 고친다 — 같은 파일이 양쪽에 배포되므로
`_redirects` 를 넣으면 루트까지 리다이렉트된다.

**✅ 처리 (2026-07-16):** `broodev-btc` Pages 프로젝트 삭제(Custom domains 에서 도메인 제거 → 프로젝트 삭제) + DNS 레코드 제거.
`curl https://btc.broodev.com/` 이 **호스트 미해석**으로 실패하는 것 확인 — 클론 소멸.
(SEO 를 더 챙기려면 삭제 대신 301 Redirect Rule 도 유효했으나, 심사 관점의 핵심인 "클론 소멸"은 삭제로 충족)

### ② `*.pages.dev` 프리뷰 도메인 → ✅ 코드로 해결
Pages 프로젝트마다 `broodev-web.pages.dev` 같은 **크롤 가능한 사이트 복제본**이 하나씩 더 있다.
**✅ 처리:** btc·voca 의 `functions/_middleware.js` 가 호스트가 `.pages.dev` 로 끝나면
`X-Robots-Tag: noindex, nofollow` 를 부착한다 (커밋 `24139e7`). 배포만으로 전 프로젝트 적용,
대시보드 작업 불필요. 코인 프로젝트들의 pages.dev 는 `_redirects` 301 이 커버(경로 기반이라 호스트 무관).
라이브 검증: pages.dev 에 헤더 있음 / 커스텀 도메인엔 없음.
(원하면 Pages → Settings → Access policy 로 프리뷰 URL 자체를 잠글 수도 있으나 필수 아님)

### ③ 코인 서브도메인 301 확인 → ✅ 라이브 확인
레포에 `apps/<coin>/_redirects` (`/* https://broodev.com/?coin=<coin> 301`) 를 넣었다.
`robots.txt` 는 **`Allow: /`** 로 두었다 — **크롤이 막히면 Google 이 301 을 못 보고 색인에서 안 빠진다.**
**✅ 확인 (2026-07-16):** `eth/xrp/doge/shib` 표본 전부 `301` + `Location: https://broodev.com/?coin=<coin>`.

### ④ 배포 watch path → ✅ 확인
`broodev-web` → Settings → **Root directory = `apps/btc`** 확인 완료.

### ⑤ (보안) `voca.broodev.com` 공개 이미지 업로드 API → ✅ 코드 구현 (활성화는 선택)
`apps/voca/functions/api/shot.js` 는 문의 폼 스크린샷용인데 **Origin/Referer 헤더 검사만** 했다.
헤더는 위조 가능하므로 누구나 광고 게재 도메인에 임의 이미지를 호스팅할 수 있었다
(2MB 제한 · 이미지 타입 검사 · 90일 TTL 은 있음).
**✅ 처리:** Turnstile 검증 코드 구현됨 — 시크릿 미설정이면 기존 동작 유지(폼 안 깨짐), 설정하면 강제(fail-closed).
**활성화 절차** (AdSense 와 무관한 보안 항목이라 필요해질 때):
1. 대시보드 → **Turnstile → Add widget** (hostname `broodev.com`, mode Managed) → Site Key / Secret Key 발급
2. `apps/voca/contact.html` 의 `TURNSTILE_SITE_KEY` 에 Site Key 기입 (공개키라 커밋 안전)
3. voca Pages 프로젝트 → Settings → Variables and secrets → `TURNSTILE_SECRET`(Secret 타입) = Secret Key

---

## 10. 앞으로의 규칙 (같은 실수 반복 금지)

> **README 에 "양보다 질 — 스팸성 양산이 아니라"라고 써두고 정확히 그 반대를 했다.**
> 이 규칙은 문서가 아니라 **강제 사항**이다.

1. **같은 앱을 파라미터만 바꿔 복제하지 않는다.**
   변형은 **한 앱 안의 옵션**(`?coin=`)으로 처리한다. 새 서브도메인은 **본질적으로 다른 기능**일 때만.
2. **얇은 페이지에 광고를 붙이지 않는다.**
   광고를 달려면 그 화면에 **고유한 게시자 콘텐츠**가 있어야 한다. 없으면 광고를 빼거나 콘텐츠를 채운다.
3. **광고 게재 화면은 `index.html` 만이 아니다.**
   `member/`, `adsense/`, 백업 폴더까지 **모든 `*.html` 을 전수 검사**한다.
   (과거 감사가 `index.html` 만 봐서 `btc/adsense/` 고아 광고 페이지를 놓쳤다)
4. **백업·스냅숏 폴더는 즉시 `noindex` + 광고 제거.** 배포되는 순간 복제 콘텐츠가 된다.
5. **새 앱에는 처음부터** 탐색 메뉴 · 개인정보처리방침 · 연락처 · 원본 콘텐츠를 갖춘다.

---

## 부록 A. 검증 방법 (재현 가능)

```bash
# 광고가 게재되면서 색인되는 모든 화면 (index.html 만이 아니라 전체 HTML)
find apps -name '*.html' | while read f; do
  grep -q 'googlesyndication' "$f" || continue
  grep -q 'content="noindex' "$f" && continue
  echo "광고+색인 → $f"
done
# 기대 결과: apps/btc/index.html, apps/voca/index.html — 이 둘만

# 색인되는 코인 페이지 (루트 1개여야 함)
for c in btc eth xrp doge bch link xlm ltc avax shib dot pepe grt sand mana; do
  grep -q 'content="noindex' apps/$c/index.html || echo "색인: $c"
done

# 낡은 canonical 잔존 확인 (0건이어야 함)
grep -rl 'canonical" href="https://btc\.broodev\.com' apps

# ⚠️ 사이트 백지 방지 — "컴파일 OK"가 아니라 "실행 OK"를 확인해야 한다 (§0-B TDZ 사건)
# btc 기본/?coin=eth/pepe/BOGUS + voca 5경로를 스텁 환경에서 실제 실행
npm i --no-save @babel/standalone   # 최초 1회
node scripts/verify-runtime.js      # 5개 전부 "실행 OK" 여야 함

# Pages Functions 검증 — pages.dev noindex 미들웨어 + shot.js Turnstile (22케이스)
node scripts/verify-functions.mjs   # "전부 통과" 여야 함
```

### 라이브 검증 (배포 후 — 2026-07-16 전수 통과한 세트)

```bash
curl -sI https://broodev.com/                    # 200 + 대시보드
curl -sI https://broodev.com/adsense/            # 301 (존 Redirect Rule — 슬래시 있는 쪽도 반드시!)
curl -sI https://broodev.com/no-such-page        # 404 (soft-404 아님)
curl -sI https://eth.broodev.com/                # 301 → broodev.com/?coin=eth
curl -sI https://btc.broodev.com/                # 호스트 미해석 (프로젝트·DNS 삭제됨)
curl -sI https://broodev-web.pages.dev/ | grep -i x-robots-tag   # noindex, nofollow
curl -sI https://broodev.com/ | grep -i x-robots-tag             # (아무것도 없어야 함)
```

## 부록 B. 관련 문서
- [`deploy-cloudflare.md`](deploy-cloudflare.md) — 루트 배포, **§1-C 코인 클론 301 절차**
- [`new-app.md`](new-app.md) — 신규 앱 플레이북
- [`../README.md`](../README.md) — 모노레포 개요
